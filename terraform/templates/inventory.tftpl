all:
  hosts:
  %{ for index, address in ipv4_public ~}
  ${hostname[index]}:
      ansible_host:   ${address} 
  %{ endfor ~}
vars:
    ansible_ssh_user: root
    ansible_ssh_private_key_file: .id_rsa